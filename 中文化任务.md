# Second Me 前端中文化任务

## 任务目标
将 Second Me 前端界面从英文改为中文，采用直接替换文案的方式（不引入 i18n 框架）。

## 项目信息
- **项目路径**: `~/Desktop/ClaudeCodeProject/second-me`
- **前端代码**: `lpm_frontend/src`
- **文件数量**: 127 个 TypeScript/TSX 文件
- **技术栈**: Next.js + React + TypeScript + Ant Design

## 需要翻译的模块

### 1. 主导航和页面标题
- Create Second Me → 创建我的分身
- Playground → 对话测试
- Second Me Services → 分身服务
- Chat Mode → 聊天模式
- Bridge Mode → 桥接模式

### 2. 训练流程
- Define Your Identity → 定义你的身份
- Upload Your Memory → 上传你的记忆
- Train Second Me → 训练分身
- Identity → 身份
- Memory Upload → 记忆上传
- Training → 训练中
- Trained → 已训练

### 3. 按钮和操作
- New Chat → 新对话
- Clear Chat → 清空对话
- Start Service → 启动服务
- Stop Service → 停止服务
- Join AI Network → 加入 AI 网络
- Save → 保存
- Next: Upload Memories → 下一步：上传记忆
- Delete Second Me → 删除分身

### 4. 设置项
- Chat Settings → 聊天设置
- Memory Retrieval → 记忆检索
- System Prompt → 系统提示词
- Temperature → 温度
- Support Model Config → 支持模型配置
- Tutorial → 教程

### 5. 表单字段
- Second Me Name → 分身名称
- Short Personal Description → 个人简介
- Email of Second Me → 分身邮箱
- Message Second Me... → 给分身发消息...

### 6. 提示文本
- "This name will represent you, and your Second Me." → "这个名字将代表你和你的分身。"
- "Briefly describe yourself: personality, motivation, or style." → "简要描述你自己：性格、动机或风格。"
- "Configure how Second Me accesses your memories" → "配置分身如何访问你的记忆"
- "Adjust creativity (0 = precise, 1 = creative)" → "调整创造性（0 = 精确，1 = 创意）"
- "Precise" → "精确"
- "Creative" → "创意"

## 实施建议

### 方案 1：逐文件替换（推荐）
1. 从主要页面开始：
   - `app/page.tsx` (首页)
   - `app/dashboard/train/*` (训练流程)
   - `app/dashboard/playground/*` (对话测试)
   
2. 然后处理组件：
   - `layouts/HeaderLayout/Header/index.tsx` (顶部导航)
   - `components/*` (各种组件)

3. 注意事项：
   - 保留所有变量名、函数名、API 字段（英文）
   - 只替换用户可见的文案
   - 保持代码格式和缩进
   - 测试每个页面确保没有遗漏

### 方案 2：全局搜索替换（快速但需谨慎）
使用 VSCode 或命令行工具批量替换常见文案，但要注意：
- 不要替换代码中的变量名
- 不要替换 API 字段名
- 不要替换文件路径

## 验收标准
1. 所有用户可见的界面文字都是中文
2. 功能正常，没有因为翻译导致的 bug
3. 保持原有的排版和样式
4. 代码逻辑和变量名保持英文

## 测试清单
- [ ] 首页显示正常
- [ ] 创建身份流程文字正确
- [ ] 上传记忆页面文字正确
- [ ] 训练页面文字正确
- [ ] 对话测试页面文字正确
- [ ] 设置面板文字正确
- [ ] 按钮和提示都是中文

## 注意事项
1. **不要动后端代码** - 只改前端 `lpm_frontend/src`
2. **不要改 API 字段** - 后端接口字段保持英文
3. **保留占位符** - 如 `{name}` 这种变量不要翻译
4. **测试后再提交** - 改完后 `npm run dev` 看看效果

---

**预计工作量**: 2-3 小时
**优先级**: 中等（功能已可用，中文化是体验优化）
